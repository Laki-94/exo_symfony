{% extends 'base.html.twig' %}

{% block title %}Acategorie{% endblock %}

{% block body %}
    <h1>Acategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acategorie.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_acategorie_index') }}">back to list</a>

    <a href="{{ path('app_acategorie_edit', {'id': acategorie.id}) }}">edit</a>

    {{ include('acategorie/_delete_form.html.twig') }}
{% endblock %}
