<form method="post" action="{{ path('app_acategorie_delete', {'id': acategorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acategorie.id) }}">
    <button class="btn">Delete</button>
</form>
